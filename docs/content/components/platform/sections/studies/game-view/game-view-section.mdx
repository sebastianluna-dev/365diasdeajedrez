---
description: "The game screen of Mis estudios: the PGN in a reducer with an undo stack, autosave through autosaveGamePgn, a close-tab warning, and the viewer, tools and aside wired to one shared PGN."
---

# `GameViewSection`

|            |                                                                                                                                                                                                                                                                                  |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| File       | `components/platform/sections/studies/game-view/game-view.section.tsx`                                                                                                                                                                                                           |
| Kind       | Client Component                                                                                                                                                                                                                                                                 |
| Stylesheet | `game-view.section.css` → block `.game-view`                                                                                                                                                                                                                                     |
| Used by    | `/estudios/[studyId]/partidas/[gameId]`                                                                                                                                                                                                                                          |
| Composes   | [`GameViewer`](/components/chess/game-viewer/game-viewer), [`GameTools`](/components/platform/sections/studies/game-view/game-tools), [`GameAside`](/components/platform/sections/studies/game-view/game-aside), [`PlatformNotice`](/components/platform/shared/platform-notice) |
| Writes     | `autosaveGamePgn()` · `services/studies/studies.actions.ts`                                                                                                                                                                                                                      |
| Uses       | `federationFlag` from `lib/chess/federations`; `GameView`, `StudyGameItem` from `services/studies/studies.types`                                                                                                                                                                 |

## Purpose

Where a game is read **and** annotated, in the same place. There is no separate editing screen: whoever owns the game plays on the board to add moves and uses the list's right click to comment, annotate, promote or delete them, and all of it saves by itself.

The PGN lives here because two things write it: the [`GameViewer`](/components/chess/game-viewer/game-viewer) (moves, arrows, structure) and the [`GameTools`](/components/platform/sections/studies/game-view/game-tools) panel under the board (comment, NAGs, review). If each kept its own copy, commenting a move would not show in the list until reload.

## Props

| Prop        | Type              | Required | Meaning                                                                                           |
| ----------- | ----------------- | -------- | ------------------------------------------------------------------------------------------------- |
| `game`      | `GameView`        | yes      | The game with `pgn`, `canEdit`, players (name, Elo, title, country), record fields and `studyId`. |
| `siblings`  | `StudyGameItem[]` | yes      | All the study's games, the current one included; they feed the aside.                             |
| `newGame`   | `ReactNode`       | no       | The new-game modal, mounted by the page with its data; absent in course databases.                |
| `editGame`  | `ReactNode`       | no       | The "Datos de la partida" modal; absent when writing is not allowed.                              |
| `errorCode` | `string`          | no       | What bounced back from an action (`gameInClasses`: delete without confirming).                    |

## Behaviour

- **PGN and history in one reducer.** `pgnReducer` holds `{ pgn, past }`; an `edit` pushes the previous PGN (ignoring no-op edits, which would spend an undo step that undoes nothing) and keeps the last 50; `undo` pops. Storing whole PGNs is a few kilobytes each and cannot drift, unlike a log of inverse operations. Ctrl+Z / ⌘Z undoes when `canEdit`, except while typing in a field, where the field's own undo rules.
- **Controlled path.** `currentPath` goes back and forth with the viewer so the evaluation chart can move the board.
- **Tools tab and focus.** `toolsTab` starts at "comment" for editors and "share" otherwise; the viewer's `onRequestEdit` switches the tab and bumps `focusRequest`, a counter rather than a boolean so two requests on the same tab are distinguishable.
- **Autosave.** With `canEdit`, 1.2 s after the last change the latest PGN goes to `autosaveGamePgn(studyId, gameId, pgn)`; a change during the wait restarts the timer. `lastSaved` is what the server confirmed; "unsaved" is derived by comparison. The label shown in the tools bar prioritises error, then saving, then unsaved, then saved.
- **Leaving.** A `beforeunload` handler warns when `pgn !== lastSaved`.
- **Players.** The internal `Player` renders the strips: colour chip, federation code with its flag from `federationFlag` (nothing painted for unknown codes), title, name, Elo. Passed by colour because the viewer can flip.
- The viewer is mounted with `controls="panel"` (in studies the game is traversed from the list), `engine`, no header (the players are already in the strips and the aside; a header stole height from the moves).

## Markup & styling

Block `.game-view` (81 lines): the aside plus board column layout and the player strips.

**Elements:** `.game-view__board`, `.game-view__chip`, `.game-view__player-country`, `.game-view__player-flag`, `.game-view__player-title`, `.game-view__player-name`, `.game-view__player-elo`

**Modifiers and states:** `.game-view__chip_side_white`, `.game-view__chip_side_black`

**Other selectors it styles:** `.platform-notice` (margin inside the column).

**Tokens:** `--platform-accent-ink`, `--platform-border-strong`, `--platform-surface`, `--platform-text`, `--platform-text-muted`, `--platform-text-subtle`

**Breakpoints:** `(max-width: 1180px)` moves the aside under the board.

## Usage

From `app/(platform)/estudios/[studyId]/partidas/[gameId]/page.tsx`:

```tsx
import { GameViewSection } from "@/components/platform/sections/studies/game-view/game-view.section";

<GameViewSection
  game={game}
  siblings={siblings}
  errorCode={error}
  editGame={
    game.canEdit ? <EditGame studyId={game.studyId} gameId={game.id} results={results} values={game} /> : undefined
  }
  newGame={
    canWrite && study ? (
      <NewGame studyId={study.id} studyName={study.name} results={results} classGames={classGames} />
    ) : undefined
  }
/>;
```

The page passes `values={game}` whole: `GameView` already contains every field of `GameFieldValues`, and listing them one by one is how the title and the federation got lost once.

## Related

- [Board & game viewer](/chess/viewer) for the editing model; [`usePgnEditing`](/components/chess/game-viewer/use-pgn-editing-hook).
- The teacher's read-only counterpart: [`StudentGameViewSection`](/components/platform/sections/teacher/students/student-game-view-section).
