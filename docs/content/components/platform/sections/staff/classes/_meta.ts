import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "staff-class-detail-section": "StaffClassDetailSection",
  "staff-classes-section": "StaffClassesSection",
};

export default meta;
