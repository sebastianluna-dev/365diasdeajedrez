---
description: "Password reset for an account through useActionState, returning the temporary password to show it once; resetting closes every session of that account."
---

# `ResetPasswordForm`

|            |                                                                                                                                                                                                                   |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| File       | `components/platform/sections/staff/students/reset-password-form.comp.tsx`                                                                                                                                        |
| Kind       | Client Component                                                                                                                                                                                                  |
| Stylesheet | `reset-password-form.comp.css` → block `.reset-password`                                                                                                                                                          |
| Used by    | [`StudentAdminDetailSection`](/components/platform/sections/staff/students/student-admin-detail-section)                                                                                                          |
| Composes   | [`TempPasswordNotice`](/components/platform/sections/staff/students/temp-password-notice), [`FormField`](/components/platform/shared/form-field), [`PlatformNotice`](/components/platform/shared/platform-notice) |
| Writes     | `resetStudentPassword()` · `services/staff-students/staff-students.actions.ts`                                                                                                                                    |
| Uses       | `ACCOUNT_INITIAL_STATE` from `services/staff-students/staff-students.types`                                                                                                                                       |

## Purpose

The same exception as [`CreateAccountForm`](/components/platform/sections/staff/students/create-account-form): the action returns the temporary password, so it uses `useActionState` and needs JavaScript. Resetting closes **all** sessions of the account, so whoever held a stolen session is out.

## Props

| Prop     | Type     | Required | Meaning                 |
| -------- | -------- | -------- | ----------------------- |
| `userId` | `string` | yes      | Sent as a hidden field. |

## Behaviour

One optional field, "Contraseña nueva": empty means a temporary one is generated and shown once. The submit is disabled while pending; results render as in the create form.

## Markup & styling

Block `.reset-password` (17 lines).

**Elements:** `.reset-password__form`

**Other selectors it styles:** `.platform-field`.

**Other classes it renders:** `.platform-button`, `.platform-button_variant_secondary`.

No tokens or media queries of its own.

## Usage

From `components/platform/sections/staff/students/student-admin-detail.section.tsx`:

```tsx
import { ResetPasswordForm } from "./reset-password-form.comp";

<ResetPasswordForm userId={student.id} />;
```

## Related

- Session invalidation in [Authentication & roles](/platform/authentication).
