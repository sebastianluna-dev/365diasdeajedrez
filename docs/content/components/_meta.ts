import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Map & rules",
  site: "Public site",
  platform: "Platform",
  auth: "Auth",
  chess: "Chess",
};

export default meta;
