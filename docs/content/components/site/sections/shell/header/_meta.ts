import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "header-section": "Header",
  "header-dropdown": "HeaderDropdown",
  "header-mobile-menu": "HeaderMobileMenu",
};

export default meta;
