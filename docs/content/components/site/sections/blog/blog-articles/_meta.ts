import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "blog-articles-section": "BlogArticles",
  "article-card": "ArticleCard",
  "category-filter": "CategoryFilter",
  pagination: "Pagination",
};

export default meta;
