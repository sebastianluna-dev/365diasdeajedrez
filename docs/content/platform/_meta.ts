import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  authentication: "Authentication & roles",
  "data-model": "Data model",
  student: "Student features",
  "teacher-and-staff": "Teacher & staff panels",
};

export default meta;
