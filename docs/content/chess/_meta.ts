import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  library: "Chess library (lib/chess)",
  viewer: "Board & game viewer",
  "engine-and-assets": "Stockfish, pieces & sounds",
};

export default meta;
