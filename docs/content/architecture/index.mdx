---
description: "How the single Next.js app is organised: route groups, request flow, routes, data access and cross-cutting concerns."
---

# Architecture

How the one Next.js application is put together, from the proxy to the section that renders.

- [Overview](/architecture/overview) — route groups and root layouts, the request flow, the directory map and the module boundaries.
- [Routes & URLs](/architecture/routing) — every URL, who can reach it, how ids look and the checklist for a new route.
- [Data access & server actions](/architecture/data-access) — the service/mapper/types triad, the Prisma and Payload clients, and the discipline every server action follows.
- [Caching, errors & logging](/architecture/caching-errors-logging) — what is cached and how it is invalidated, error boundaries, and the JSON logger.
