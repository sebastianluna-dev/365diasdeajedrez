import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  overview: "Overview",
  routing: "Routes & URLs",
  "data-access": "Data access & server actions",
  "caching-errors-logging": "Caching, errors & logging",
};

export default meta;
