import { BoardLoading } from "@/components/platform/shared/board-loading.comp";

// The studies list and everything under it that has no boundary of its own.
export default function StudiesLoading() {
  return <BoardLoading />;
}
